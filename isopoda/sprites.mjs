import {STAGES} from './phenotypes.mjs?v=morphology-6';
export const MOLT_REGIONS={anterior:['cephalon','p1','p2','p3','p4'],posterior:['p5','p6','p7','pleon','pleotelson','uropods']};
export const POSTURES=['normal','resting','probing','tucked','curled','turning','feeding','grooming','molting','emerging','swimming'];
export function stableHash(seed){let x=2166136261;for(const c of String(seed))x=Math.imul(x^c.charCodeAt(0),16777619);return x>>>0}
export function resolvePalette(p){const v={...p};for(const [key,parent] of [['cephalon','tergite'],['epimera','tergite'],['pleon','tergite'],['pleotelson','pleon'],['uropods','pleotelson'],['antennae','epimera'],['legs','epimera']])v[key]??=v[parent];return v}
const aliases={S:'juvenile',M:'subadult',L:'adult'};
export function renderModel(visual,{stage='adult',condition='normal',seed=0,moving=false}={}){stage=aliases[stage]||stage;const growth=visual.stageProfiles?.[stage]||STAGES[stage]||STAGES.adult;const posture=typeof condition==='object'?condition.posture||'normal':condition;const molt=typeof condition==='object'?condition.molt||'none':condition.startsWith('molt-')?condition.slice(5):'none';return {visual,stage:STAGES[stage]?stage:'adult',growth,palette:resolvePalette(visual.palette),seed,variant:stableHash(seed)%4,posture,molt,moving:posture==='moving'||moving};}
const mix=(a,b,t)=>{const rgb=c=>c.slice(1).match(/../g).map(x=>parseInt(x,16));return '#'+rgb(a).map((v,i)=>Math.round(v*(1-t)+rgb(b)[i]*t).toString(16).padStart(2,'0')).join('')};
function regionPalette(m,region,n){const p=m.palette;let base=p[region==='pereon'?'tergite':region]||p.tergite,edge=p.epimera,tip=edge;for(const r of m.visual.patterns){const c=p[r.color]||r.color||p.dark;if(r.type==='trizone'){base=p[r.colors[region==='cephalon'||region==='pereon'&&n<=2?0:region==='pereon'&&n<=5?1:2]];edge=base;tip=base}if(r.target==='epimera'){if(r.type==='epimeraTip')tip=c;else edge=tip=c}if([region,'body','any'].includes(r.target)&&(['solid','headMask','posteriorPatch'].includes(r.type)||r.type==='saddle'&&(r.segments||[3,4,5]).includes(n)||r.type==='segmentBand'&&(r.segments||[2,5]).includes(n)))base=c;}return {base,edge,tip};}
export function pixelAnatomy(m,{posture='normal',molt='none',phase=0,moving=m.moving}={}){const v=m.visual,p=m.palette,h=stableHash(m.seed),frame=phase%4;const full=posture==='curled'&&v.conglobation.ability==='full';const tucked=posture==='tucked'||posture==='curled';const swimming=posture==='swimming';const closure=posture==='curled'?1:.55;const bend=swimming?[-.20,.08,.20,-.08][frame]:posture==='turning'?.45:posture==='grooming'?-.3:0;const modules=[];function module(region){const cells=[];modules.push({region,cells});return (x,y,color)=>cells.push([Math.round(x),Math.round(y),color])}const pale=region=>MOLT_REGIONS[molt]?.includes(region);function color(c,region){return pale(region)?mix(c,'#eee6d1',.58):c}function line(put,x,y,xx,yy,c){const d=Math.ceil(Math.max(Math.abs(xx-x),Math.abs(yy-y)));for(let i=0;i<=d;i++)put(x+(xx-x)*i/(d||1),y+(yy-y)*i/(d||1),c)}function ribbon(put,x,y,xx,yy,c,thickness=1){line(put,x,y,xx,yy,c);if(thickness>1)line(put,x,y+1,xx,yy+1,c);if(thickness>2)line(put,x,y-1,xx,yy-1,c)}const width=Math.round(12*v.body.width*m.growth.widthRatio),stride=3.6*v.body.length;const center=n=>({x:Math.round(10-(n-1)*stride),y:bend*Math.round((n-4)*(n-4)/5)+(swimming?Math.round(Math.sin((n+frame)*1.08)*1.35):0)});if(full){const r=width+2;for(let n=7;n>=1;n--){const put=module('p'+n),pal=regionPalette(m,'pereon',n);const x0=Math.round(-r+(7-n)*2*r/7),x1=Math.round(-r+(8-n)*2*r/7);for(let xx=x0-2;xx<x1+2;xx++)for(let y=-r;y<=r;y++){const shift=Math.round(y*y/(r*r)*2),x=xx+shift;if(xx<x0||xx>=x1||x*x+y*y>=r*r)continue;const c=(x+1)*(x+1)+y*y>(r-2)*(r-2)?pal.edge:(xx===x0&&Math.abs(y)>2)?mix(pal.base,'#201e1a',.3):y<0&&x%3===1?mix(pal.base,'#fff1cc',.24):pal.base;put(x,y,color(c,'p'+n))}}return modules;}const front=14,back=center(7).x-7,projection=v.body.projection||{middle:.60,frontWidth:.60,rearWidth:.48},cap=(1-projection.middle)/2;const envelope=x=>{const t=(x-back+.5)/(front+5-back),q=t<cap?t/cap:t>1-cap?(1-t)/cap:1;const roundness=t<cap?(projection.rearRoundness||1):(projection.frontRoundness||1);return width*Math.pow(Math.sqrt(Math.max(0,1-(1-Math.min(1,q))**2)),roundness)};
const leg=module('legs'),legInk=mix(p.legs,'#353c2d',.55),walkCycle=[-1,0,1,0];for(let n=1;n<=7;n++){const c=center(n);for(const s of [-1,1]){const locomoting=moving||posture==='turning'||posture==='emerging'||swimming;const gait=(frame+(n%2?0:2)+(s>0?1:3))%4;const idleGait=(frame+n*2+(s>0?2:0))%7;const active=!tucked&&(posture==='resting'?idleGait===0:locomoting?true:idleGait<1);const edge=Math.round(envelope(c.x))-1,exposure=v.legs.visibility??.7;const reachBase=Math.max(1,Math.round(1+(v.legs.length||.35)*5));const reachPulse=locomoting?(gait===1?2:gait===2?1:0):0;const reach=active?Math.min(7,reachBase+reachPulse+Number(posture==='turning'&&s>0)):-1;const attachInset=Math.max(2,Math.round(5-exposure*2.5));const strideAmp=Math.max(2,Math.round(2.7*(v.legs.stepScale||1)*(swimming?1.35:1)));const step=active?Math.round((locomoting?walkCycle[gait]*strideAmp:0)+(posture==='turning'&&s>0?1:0)):0;const thickness=Math.max(1,Math.round(v.legs.thickness||1));ribbon(leg,c.x+1,c.y+s*(edge-attachInset),c.x+step,c.y+s*(edge+reach),legInk,thickness);}}
const rear=center(7),pleon=module('pleon');const pleonLen=Math.max(3,Math.round(3+v.pleon.length*8));for(let n=0;n<pleonLen;n++){const x=rear.x-n-1,envelopeW=Math.round(envelope(x));const taper=1-(n/Math.max(1,pleonLen-1))*v.pleon.taper*.55;const target=Math.max(2,Math.round(width*(.55+v.pleon.width*.35)*taper));const w=Math.max(2,Math.min(envelopeW,target));for(let y=-w;y<=w;y++){const seam=n%2?0:v.pleon.segmentContrast||.2;const col=Math.abs(y)>=w?mix(p.pleon,'#292a24',.22):seam?mix(p.pleon,'#292a24',seam):p.pleon;pleon(x,rear.y+y,color(col,'pleon'))}}const telson=module('pleotelson'),pt=v.pleotelson||{},telShape=pt.shape||pt.apex||'triangular-concave';const telLen=Math.max(2,Math.round((2+(pt.length||.14)*10)*(pt.lengthScale||1)));const telBase=Math.max(2,Math.round(width*(.20+(pt.width||.30)*.38)*(pt.widthScale||1)));const telStart=rear.x-pleonLen-1;for(let i=0;i<telLen;i++){const u=i/Math.max(1,telLen-1);let factor=1-u*.55;if(telShape==='triangular-concave')factor=Math.pow(1-u,.78)*.86+.14;else if(telShape==='triangular-broad')factor=1-u*.62;else if(telShape==='triangular-long')factor=1-u*.82;else if(telShape==='pointed'||telShape==='keeled-pointed')factor=1-u*.92;else if(telShape==='trapezoidal')factor=1-u*.24;else if(telShape==='hourglass')factor=u<.55?1-u*.72:.60+(u-.55)*.28;else if(telShape==='compact')factor=1-u*.34;let w=Math.max(1,Math.round(telBase*factor));if(['pointed','keeled-pointed','triangular-long'].includes(telShape)&&i===telLen-1)w=0;if(pt.apex==='truncate'&&i===telLen-1)w=Math.max(2,w);const xx=telStart-i;for(let y=-w;y<=w;y++){let col=p.pleotelson;if(Math.abs(y)>=w&&w>0)col=mix(col,'#292a24',.22);if(i===0)col=mix(col,p.light,.10);if(pt.keel&&Math.abs(y)<=0&&i>0)col=mix(col,p.light,.24);telson(xx,rear.y+y,color(col,'pleotelson'))}}const tail=module('uropods'),ur=v.uropods||{},mode=ur.mode||'projecting';if(ur.visibility>.2&&!tucked){const telTip=telStart-(telLen-1),thick=Math.max(1,Math.round((.65+(ur.thickness||.15)*3)*(ur.thicknessScale||1)));for(const s of [-1,1]){const attachY=rear.y+s*(telBase+1),ink=color(p.uropods,'uropods');if(mode==='projecting'){const extra=Math.max(1,Math.round((ur.projection||.2)*7));const targetY=rear.y+s*(telBase+1+Math.round((ur.spread||.2)*5));ribbon(tail,telStart+1,attachY,telTip-extra,targetY,ink,thick)}else if(mode==='compact'){const targetY=rear.y+s*(telBase+1+Math.round((ur.spread||.1)*2));ribbon(tail,telStart+1,attachY,telTip-1,targetY,ink,thick)}else if(mode==='flush-exopod'){ribbon(tail,telStart+1,attachY,telTip+1,rear.y+s*Math.max(2,telBase-1),ink,Math.max(2,thick))}else if(mode==='flush-protopod'){const y0=rear.y+s*Math.max(2,telBase-1);ribbon(tail,telStart+2,y0,telTip+1,y0,ink,Math.max(2,thick));tail(telStart+1,rear.y+s*telBase,ink)}}}
for(let n=7;n>=1;n--){const c=center(n),region='p'+n,put=module(region),epi=module('epimera'+n),pal=regionPalette(m,'pereon',n);const plateWidth=Math.max(3,Math.round(stride+1));const cx=c.x;if(tucked)c.y+=Math.round((n-4)*(n-4)/12*closure);for(let x=0;x<plateWidth;x++){const notch=x===plateWidth-2?1:0;const half=Math.round(envelope(cx+x))-notch;for(let y=-half;y<=half;y++){const sideDepth=Math.max(1,Math.round(v.pereon.epimera.width*(1-v.body.convexity*.55)*3));let col=pal.base;const rh=stableHash(m.seed+':'+n),ny=(y+half)/(half*2);for(const rule of v.patterns){if(!['pereon','body','any'].includes(rule.target))continue;const ink=p[rule.color]||rule.color||p.dark;if(rule.type==='blotch'&&((Math.abs(y-((rh%9)-4))<2+(rh%2)&&x<2)||(Math.abs(y-(((rh>>>7)%11)-5))<2&&x>0)))col=ink;if(['dorsalStripe','centerField'].includes(rule.type)&&Math.abs(y)<Math.max(1,half*(rule.width||.35)))col=rule.opacity?mix(col,ink,rule.opacity):ink;if(rule.type==='lateralStripe'&&Math.abs(Math.abs(y)-half*.65)<1.5)col=ink;if(rule.type==='spotRow'&&[.25,.5,.75].some(a=>Math.abs(ny-a)<.06)&&x===1)col=ink;if(rule.type==='spot'&&Math.abs(y-(rh%7-3))<2&&x<2)col=ink}if(x===0&&(Math.abs(y)>half*.6||y%4===0))col=mix(col,'#1d231c',.35);else if(x<3&&y>-half+2&&y<-half+5&&(n+h)%3!==0)col=mix(col,'#fff4d8',.25);if(v.surface.sculpture==='tuberculate'&&Math.abs(y)<half-1){const spacing=v.surface.tubercleSpacing||3;if((x*2+y+n+h)%spacing===0)col=mix(col,p.light,.18+.28*(v.surface.intensity||.5));else if((x*2+y+n+h+1)%spacing===0)col=mix(col,'#1b201a',.12+.16*(v.surface.intensity||.5))}else if(v.surface.scaleSetae!=='none'&&(x*3+y+n+h)%11===0&&Math.abs(y)<half-1){col=mix(col,p.light,v.surface.scaleSetae==='coarse'?.16:.07)}if((h>>>4)%7===n&&y===2&&x===2)col=mix(col,p.light,.5);if(Math.abs(y)>half-sideDepth)col=mix(x<=1?pal.tip:pal.edge,'#252b23',.20+v.body.convexity*.16+(x===0?.15:0));else if(Math.abs(y)>half*.65)col=mix(col,'#252b23',v.body.convexity*.24);const arc=Math.round((1+v.pereon.plateArc)*(y/half)**2);if(x===arc+1&&Math.abs(y)<half-2&&(y+n)%3!==0)col=mix(col,p.light,.22);if((x+y+n+h)%7===0&&Math.abs(y)<half-1)col=mix(col,'#eee4d0',.10);if(x===arc&&Math.abs(y)<half-1)col=mix(col,'#252b23',.24);(Math.abs(y)>half-sideDepth?epi:put)(cx+x,c.y+y,color(col,region))}}const e=v.pereon.epimera,skirt=e.skirt||0,skirtPut=module('skirt'+n);const coros=e.edgeProfile==='stepped-serrate';const corosSegmentScale=coros?[.76,.91,1,1.05,1,.91,.76][n-1]:1;for(const side of [-1,1])for(let x=0;x<plateWidth-1;x++){const edge=Math.round(envelope(cx+x)),u=x/Math.max(1,plateWidth-2);let shape=Math.sin((u*.8+.1)*Math.PI);if(e.lobe==='swept')shape=1-u*.75;else if(coros)shape=.58+Math.sin(u*Math.PI)*.48;else if(e.lobe==='shield')shape=.82+Math.sin((u*.9+.05)*Math.PI)*.22;else if(e.lobe==='rectangular')shape=.92-u*.08;const posteriorOn=!e.posteriorProjectionFrom||n>=e.posteriorProjectionFrom;const posterior=posteriorOn?(e.posteriorProjection||0)*Math.pow(1-u,2):0;const reach=tucked?0:Math.max(0,Math.round((skirt*3*shape*corosSegmentScale)+posterior*3));for(let d=0;d<reach;d++){const ink=d===reach-1?pal.tip:pal.edge;skirtPut(cx+x,c.y+side*(edge+d),color(mix(ink,d===0?'#eee1be':'#252b23',d===0?.12:.20),region))}}if(v.surface.noduliLaterales==='linear'){const nod=module('noduli'+n),edge=Math.max(2,Math.round(envelope(cx+1))-2),offset=n===1?Math.max(2,edge-2):edge;for(const side of [-1,1])nod(cx+1,c.y+side*offset,color(mix(pal.base,p.light,.42),region))}}
const head=module('cephalon'),hx=tucked?12:14,hy=posture==='feeding'?2:posture==='emerging'?-2:bend?2:0;const hp=regionPalette(m,'cephalon'),ct=v.cephalon||{},hw=Math.max(3,Math.round(width*(.58+(ct.width||.55)*.18)));const headLen=Math.max(4,Math.round(3+(ct.length||.7)*3)),rounded=(ct.shape||'').includes('rounded');for(let ix=0;ix<headLen;ix++){const u=ix/Math.max(1,headLen-1),frontRound=rounded?Math.round(u*u*(1+(ct.roundness||.5)*1.4)):Math.round(u*u*.7);const half=Math.max(2,hw-frontRound),xx=hx+ix;for(let y=-half;y<=half;y++){let col=hp.base;if(Math.abs(y)>=half)col=mix(col,'#222820',.22);if(ix===0)col=mix(col,p.light,.08);head(xx,hy+y,color(col,'cephalon'))}}const frontX=hx+headLen,headShape=ct.shape||'trilobed';if(headShape.startsWith('trilobed')){const md=Math.max(1,Math.round((ct.medianProjection||.2)*4)),ld=Math.max(1,Math.round((ct.lateralProjection||.3)*3));const ly=Math.max(2,Math.round(hw*(ct.lateralLobes||.55)));for(let d=0;d<md;d++)for(let y=-1;y<=1;y++)head(frontX+d,hy+y,color(hp.base,'cephalon'));for(const s of [-1,1])for(let d=0;d<ld;d++){head(frontX+d,hy+s*ly,color(hp.base,'cephalon'));head(frontX+d,hy+s*Math.max(1,ly-1),color(mix(hp.base,'#222820',d===ld-1?.10:0),'cephalon'))}}if(ct.scutellum==='triangular'){for(let d=0;d<3;d++)for(let y=-(2-d);y<=2-d;y++)head(frontX+d,hy+y,color(d===0?mix(hp.base,p.light,.14):hp.base,'cephalon'))}else if(ct.scutellum==='subtle'||ct.scutellum==='notched-subtle'){for(let y=-1;y<=1;y++)if(!(ct.scutellum==='notched-subtle'&&y===0))head(frontX,hy+y,color(mix(hp.base,p.light,.10),'cephalon'))}if(!tucked)for(const s of [-1,1]){const eyeY=hy+s*Math.max(2,Math.round(hw*(ct.eyeSet||.78)));head(hx+Math.max(1,headLen-2),eyeY,'#20291f');if((ct.eyeScale||1)>1.15)head(hx+Math.max(1,headLen-3),eyeY,'#20291f')}
const antenna=module('antennae'),length=Math.round((4+v.antennae.length*6)*(1+((h>>>8)%5-2)*.025)*m.growth.appendageRatio);for(const s of [-1,1]){const startX=frontX-1,startY=hy+s*Math.max(2,Math.round(hw*.64));const probe=posture==='probing'||posture==='emerging',groom=posture==='grooming';const antPhase=(frame+(s>0?0:2))%4;const walkSweep=moving?[0,.30,0,-.30][antPhase]:probe?[0,.10,.02,-.08][antPhase]:groom?[.06,-.06,.10,-.08][antPhase]:0;let angle=s*((posture==='resting'?.20:.38)+v.antennae.spread*.5+walkSweep);const walkFold=[0,.24,.04,-.22][frame],tipFold=[.04,-.18,.20,-.04][frame];const folds=tucked?[1.0,.9]:groom?[.95,.8]:probe?[.08+walkSweep*.35,.08-walkSweep*.2]:moving?[v.antennae.bend+walkFold,.24+tipFold]:[v.antennae.bend,.24];let x=startX,y=startY;for(let j=0;j<3;j++){if(j)angle+=s*folds[j-1];const len=length*(v.antennae.joints||[.44,.32,.24])[j]*(tucked?.6:1),xx=x+Math.cos(angle)*len,yy=y+Math.sin(angle)*len;const antThickness=j===0&&v.antennae.thickness>.19?2:1;ribbon(antenna,x,y,xx,yy,p.antennae,antThickness);if(j===0&&v.antennae.peduncleArticle3Tooth){const t=.56,tx=x+(xx-x)*t,ty=y+(yy-y)*t,tooth=v.antennae.peduncleArticle3Tooth==='long-wide'?2:1;const px=-Math.sin(angle)*s,py=Math.cos(angle)*s;ribbon(antenna,tx,ty,tx+px*tooth,ty+py*tooth,p.antennae,tooth>1?2:1)}x=xx;y=yy}}if(posture==='feeding'){const mouth=module('mouthparts');mouth(frontX,hy+(frame%2),'#d0ba83');mouth(frontX-1,hy+2,p.dark)}if(posture==='molting'){const old=module('exuvia');const back=molt!=='anterior';for(let i=0;i<5;i++)old((back?-12:12)+i,-width-3+i%2,'#bdbca2')}return modules;}
export function exuviaPixels(m,{phase='whole',age=0}={}){
 const cells=new Map(),cut=-4;
 for(const part of pixelAnatomy(m,{posture:'normal',molt:'none',phase:0,moving:false})){
  for(const [x,y] of part.cells){
   if(phase==='anterior'&&x<cut)continue;
   if(phase==='posterior'&&x>=cut)continue;
   cells.set(x+','+y,{x,y,region:part.region});
  }
 }
 const fade=Math.max(.38,.86-Math.max(0,age)*.055),has=(x,y)=>cells.has(x+','+y),out=[];
 for(const cell of cells.values()){
  const {x,y,region}=cell,edge=!has(x-1,y)||!has(x+1,y)||!has(x,y-1)||!has(x,y+1);
  const appendage=['legs','antennae','uropods'].includes(region),plate=/^(p|epimera|skirt|noduli)\d/.test(region);
  const n=Number((region.match(/\d+/)||['0'])[0]);
  const rgb=edge?[239,238,221]:appendage?[186,188,174]:plate&&n%2?[218,218,202]:[207,208,193];
  const alpha=fade*(edge?1:(appendage?0.70:0.79));
  out.push([x,y,`rgba(${rgb[0]},${rgb[1]},${rgb[2]},${alpha.toFixed(3)})`]);
 }
 return out;
}
const models=new WeakMap(),renderedStates=new WeakMap();export function makeIsopod(species,options={}){const m=renderModel(species.visual||species,options),bug=document.createElement('span');bug.className='isobug isopod';bug.setAttribute('aria-hidden','true');for(const [k,v] of Object.entries({stage:m.stage,ability:m.visual.conglobation.ability,variant:m.variant,seed:m.seed}))bug.dataset[k]=v;const size=Math.round(80*m.growth.scale);bug.style.width=size+'px';bug.style.height=size+'px';const canvas=document.createElement('canvas');canvas.width=canvas.height=64;canvas.className='pixel-body';bug.append(canvas);const body=document.createElement('b');body.className='body';for(const name of ['legs','antennae']){const node=document.createElement('i');node.className=name;bug.append(node)}for(const region of ['cephalon',...Array.from({length:7},(_,i)=>'p'+(i+1)),'pleon','pleotelson','uropods']){const part=document.createElement('i');part.dataset.region=region;part.className='anatomy '+(/^p\d$/.test(region)?'plate '+region:region);part.style.setProperty('--base',regionPalette(m,/^p\d$/.test(region)?'pereon':region,Number(region[1])).base);if(/^p\d$/.test(region)){const epimera=document.createElement('i');epimera.className='epimera';part.append(epimera)}body.append(part)}bug.append(body);models.set(bug,{m,canvas,cache:new Map()});setIsopodState(bug,{posture:m.posture,molt:m.molt,moving:m.moving});return bug}
export function setIsopodState(bug,{posture='normal',molt='none',moving=false,phase=0,angle=0,occlusion=0,occlusionSide='rear'}={}){const model=models.get(bug);if(!model)return;if(posture==='emerging'&&occlusion===0&&!moving)occlusion=.48;if(posture.startsWith('molt-')){molt=posture.slice(5);posture='molting'}if(!POSTURES.includes(posture))posture='normal';const orientation=Math.round(angle/(Math.PI/32)),f=Math.floor(phase)%4,key=[posture,molt,moving,f,orientation,occlusionSide,Math.round(occlusion*8)].join(':');if(renderedStates.get(bug)===key)return;renderedStates.set(bug,key);bug.dataset.posture=posture;bug.dataset.molt=molt;bug.dataset.phase=f;bug.dataset.occlusion=occlusion.toFixed(2);bug.classList.toggle('moving',moving);for(const part of bug.querySelectorAll('[data-region]'))part.classList.toggle('molting',(MOLT_REGIONS[molt]||[]).includes(part.dataset.region));const {m,canvas,cache}=model,sourceKey=[posture,molt,f,moving].join(':');let cells=cache.get(sourceKey);if(!cells){cells=new Map();for(const part of pixelAnatomy(m,{posture,molt,phase:f,moving}))for(const [x,y,c] of part.cells)cells.set((y+32)*64+x+32,c);cache.set(sourceKey,cells);if(cache.size>64)cache.delete(cache.keys().next().value)}const ctx=canvas.getContext('2d');ctx.clearRect(0,0,64,64);ctx.imageSmoothingEnabled=false;const a=orientation*Math.PI/32,cos=Math.cos(a),sin=Math.sin(a);for(let y=0;y<64;y++)for(let x=0;x<64;x++){const sx=Math.round((x-32)*cos+(y-32)*sin),sy=Math.round(-(x-32)*sin+(y-32)*cos);if(occlusion>0&&(occlusionSide==='front'?sx>24-occlusion*49:sx< -24+occlusion*49))continue;if(sx< -32||sx>=32||sy< -32||sy>=32)continue;const c=cells.get((sy+32)*64+sx+32);if(c){ctx.fillStyle=c;ctx.fillRect(x,y,1,1)}}}
export function makeBug(species,index=0){return makeIsopod(species,{seed:index})}
